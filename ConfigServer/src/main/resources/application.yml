#spring.application.name=ConfigServer

server:
  port: 8085
spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/SonuKumar808/microservice-config-server
          clone-on-start: true